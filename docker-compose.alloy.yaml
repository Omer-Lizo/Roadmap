services:
  web:
    image: python:3.12-bookworm
    working_dir: /workspace
    network_mode: host
    volumes:
      - .:/workspace
    command: bash -lc "python -m http.server 8000 --bind 0.0.0.0"
